<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>UN Countries</title>
    <link rel="stylesheet" href="/css/main.css" />
  </head>
  <body tabindex="-1">
    <%- include('partials/navbar', {page: ''})%>
    <main class="pb-4">
      <div class="container mx-auto px-4">
        <div class="hero bg-base-200 rounded-lg p-4">
          <div class="hero-content text-center">
            <div class="max-w-md">
              <h1 class="text-5xl font-bold">Edit Country: <%= country.name %></h1>
              <p class="py-6">
                Use the below form to update the Country: "<%=
                country.name %>"
              </p>
            </div>
          </div>
        </div>

        <br />

        <form method="post" action="/un/editCountry">
          <div
            class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 gap-y-0"
          >
            <div>
              <div class="mb-6">
                <label for="name" class="blockfont-medium"
                  >Name</label
                >
                <input
                  type="text"
                  id="name"
                  name="name"
                  value="<%= country.name %>"
                  required
                  class="w-full border border-gray-300 rounded-md px-3 py-2 focus:border-blue-300"
                />
              </div>
              <div class="mb-6">
                <label for="regionId" class="blockfont-medium"
                  >Region</label
                >
                <select
                  id="regionId"
                  name="regionId"
                  required
                  class="w-full border border-gray-300 rounded-md px-3 py-2 focus:border-blue-300"
                >
                <% regions.forEach(region=>{ %>
                    <option <%= (country.regionId == region.id) ? "selected" : "" %> value="<%= region.id %>">
                      <%= region.name %>
                    </option>
                  <% }) %>
                  
                </select>
              </div>
              <div class="mb-6">
                <label for="population" class="blockfont-medium"
                  >Population</label
                >
                <input
                  type="number"
                  id="population"
                  name="population"
                  value="<%= country.population %>"
                  required
                  class="w-full border border-gray-300 rounded-md px-3 py-2 focus:border-blue-300"
                />
              </div>
            </div>
            <div>
              <div class="mb-6">
                <label for="official" class="blockfont-medium"
                  >Official Name</label
                >
                <input
                  type="text"
                  id="official"
                  name="official"
                  value="<%= country.official %>"
                  required
                  class="w-full border border-gray-300 rounded-md px-3 py-2 focus:border-blue-300"
                />
              </div>
              <div class="mb-6">
                <label for="a2code" class="blockfont-medium"
                  >Alpha 2 Code</label
                >
                <input
                  type="text"
                  id="a2code"
                  name="a2code"
                  value="<%= country.a2code %>"
                  style="background-color: lightgrey;"
                  required
                  readonly
                  class="w-full opacity-50 cursor-not-allowed border border-gray-300 rounded-md px-3 py-2 focus:border-blue-300"
                />
              </div>
              <div class="mb-6">
                <label for="languages" class="blockfont-medium"
                  >Languages</label
                >
                <input
                  type="text"
                  id="languages"
                  name="languages"
                  value="<%= country.languages %>"
                  required
                  class="w-full border border-gray-300 rounded-md px-3 py-2 focus:border-blue-300"
                />
              </div>
            </div>
            <div>
              <div class="mb-6">
                <label for="nativeName" class="blockfont-medium"
                  >Native Name</label
                >
                <input
                  type="text"
                  id="nativeName"
                  name="nativeName"
                  value="<%= country.nativeName %>"
                  required
                  class="w-full border border-gray-300 rounded-md px-3 py-2 focus:border-blue-300"
                />
              </div>
              <div class="mb-6">
                <label for="capital" class="blockfont-medium"
                  >Capital</label
                >
                <input
                  type="text"
                  id="capital"
                  name="capital"
                  value="<%= country.capital %>"
                  required
                  class="w-full border border-gray-300 rounded-md px-3 py-2 focus:border-blue-300"
                />
              </div>
              <div class="mb-6">
                <label
                  for="permanentUNSC"
                  class="blockfont-medium"
                  >In Permanent UNSC Member?</label
                >
                <label class="label cursor-pointer justify-center">
                  <input
                    type="checkbox"
                    name="permanentUNSC"
                    class="checkbox"
                    <%= country.permanentUNSC ? "checked" : "" %>
                  />
                  <span class="label-text px-2"></span>
                </label>
              </div>
            </div>
          </div>
          <div
            class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 gap-6 gap-y-0"
          >
            <div class="mb-6">
              <label for="wikipediaURL" class="blockfont-medium"
                >Wikipedia URL</label
              >
              <input
                type="url"
                id="wikipediaURL"
                name="wikipediaURL"
                value="<%= country.wikipediaURL %>"
                required
                class="w-full border border-gray-300 rounded-md px-3 py-2 focus:border-blue-300"
              />
            </div>
            <div class="mb-6">
              <label for="flag" class="blockfont-medium"
                >Flag Image URL</label
              >
              <input
                type="url"
                id="flag"
                name="flag"
                value="<%= country.flag %>"
                required
                class="w-full border border-gray-300 rounded-md px-3 py-2 focus:border-blue-300"
              />
            </div>
          </div>
          <div
          class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 gap-6 gap-y-0"
        >
          <div class="mt-6">
            <button type="submit" class="btn btn-secondary w-full">Update Country</button>
          </div>
          <div class="mt-6">
            <a href="/un/deleteCountry/<%= country.a2code %>" class="btn btn-error w-full">Delete Country</a>
          </div>
          </div>
        </form>
      </div>
    </main>
  </body>
</html>
